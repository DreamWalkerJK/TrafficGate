# Test report

`dotnet build TrafficGate.slnx --no-restore` passes on .NET SDK 10.0.102 with 0 warnings and 0 errors. `dotnet test TrafficGate.slnx --no-restore` passes 37/37 tests. The suite includes SQLite snapshot recovery and ETag concurrency, route and destination validation, limiter quotas/queues/generations, authorization and trusted-header behavior, upstream error preservation, timeout and cancellation, request limits, SSE, WebSocket, and cleartext HTTP/2 framed gRPC forwarding on real Kestrel hosts. The portable `tests/protocol-smoke.ps1` script is checked in; one local run was blocked by a pre-existing process occupying port 5090. Helm lint/template and cluster runtime tests remain pending because no Helm executable or Kubernetes cluster is installed in this environment.
